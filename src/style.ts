import styled from 'styled-components';
import { motion } from 'framer-motion';

const PageScaffoldContainer = styled(motion.div)`
  width: 100vw;
  height: 100vh;
  position: fixed;
  padding: 0px 10vw;
`;

export { PageScaffoldContainer };